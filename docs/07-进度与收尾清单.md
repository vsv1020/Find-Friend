# 进度与收尾清单

> 更新:2026-08-24 · 对应 `02-任务拆解与落地方案.md` 的任务编号
> **这份清单是对着代码核对出来的,不是照着计划抄的。**

---

## 一句话现状

**PRD §4.1 的 V1 必做五项(发局、报名、成团通知、临时群聊、人工审核后台)已全部实现**,
另外多做了海报、评价、合规与注销。**136 项单元测试全过。**

**剩下的阻塞项全部是外部依赖**(备案、法务、AppID),不是开发。
代码侧只剩一个上线阻塞项:**举报入口(T22)**。

---

## 二、已完成

### 领域逻辑(`cloudfunctions/common/`,纯函数,全部有测试)

| 模块 | 职责 | 关键约束 |
|---|---|---|
| `state-machine` | 9 状态流转 | 终态无出边;非法流转抛错而非静默忽略 |
| `formation` | 成团判定、催报名 | 幂等;判定 6h(D04);催报名有最小提前量防通知打架 |
| `signup` | 名额、候补、局主报名 | D07 配比代码路径保留但关闭;局主不能单独退出 |
| `reliability` | 爬约计数、评价资格、分数 | **事实认定与主观评价严格分离** |
| `metrics` | 成团率、双指标 | 无样本返回 null 而非 0 |
| `chat` | 群聊权限、轮询退避 | 候补进不去;归档后只读 |
| `moderation` | 内容安全判读与降级 | **按传播面分级**,非一刀切 |
| `schedule` | 周末快捷时段 | 时区不依赖运行环境;周标签按中文周一起始 |
| `sharecode` | 分享短码 | 绕开小程序码 scene 的 32 字符上限 |
| `anonymize` | 注销去标识化 | `verifyAnonymized` 供上线前自查 |

### 云函数(8 个)

`events` · `signups` · `formation`(定时) · `admin` · `poster` · `rating` · `chat` · `account`

### 小程序页面(10 个)

局列表 · 局详情 · 发局 · 报名成功 · 海报 · 行前沟通 · 评价 · 我的 · 账号与隐私 · 运营后台

### 对照 `02` 的任务清单

| 任务 | 状态 | 备注 |
|---|---|---|
| T01 项目基建 | ✅ | 小程序 + 云开发 + 配置同步(自动发现云函数目录) |
| T02 数据模型 | ✅ | 集合、索引、权限;`init-db.js` 打印建库指令 |
| ~~T03 白名单 POI~~ | ⛔ 取消 | D09 改为自由输入,保存坐标以保留场地热度维度 |
| T04 局详情页 | ✅ | 未登录可浏览;只显示计数不显示身份 |
| T05 报名流 | ✅ | `getPhoneNumber` 一键授权 |
| T06 通知通道 | ⚠️ 部分 | 通知已入队并去重,**订阅消息模板 ID 待填**;短信兜底 V1.0 未开 |
| T07 报名成功页 + 推荐位 | ✅ | 含订阅消息授权引导 |
| T08 运营后台 v0 | ✅ | 手机可用;审核 ≤3 次点击 |
| T09 埋点 v0 | ✅ | 未登录带 anonId |
| T10 合规文件 v0 | ✅ | 见 T30 |
| T11 发局表单 | ✅ | 周末快捷时段;选场景带出全部默认值 |
| T12 局状态机 | ✅ | 全部状态与边有测试 |
| T13 审核队列 | ✅ | D06 全局开关 + 局主免审白名单 |
| T14 成团引擎 | ✅ | 15 分钟定时;条件更新 + dedupeKey 保证幂等 |
| T15 通知体系 | ⚠️ 部分 | 四类模板已入队,**实际发送待模板 ID** |
| T16 名额与候补 | ✅ | 递补自动触发通知 |
| T17 行前沟通 | ✅ | 轮询 + 退避;归档转只读 |
| T18 靠谱度评价 | ✅ | 局主事实认定与参与者互评分离 |
| T19 我的 / 个人中心 | ✅ | 含待评价入口与注销 |
| T20 埋点补全 | ✅ | 双指标可算 |
| T25 推荐位算法 | ✅ | 同场景 > 同时段 > 差人数最少 |
| T27 分享海报 | ✅ | 小程序码带缓存 |
| T28 内容安全 | ✅ | 群聊 / 昵称 / 发局文案,按传播面分级 |
| T30 合规四件套 | ✅ 草稿 | **待两地法务审阅** |

---

## 三、代码侧还剩什么

| # | 任务 | 为什么 | 估时 |
|---|---|---|---|
| **T22** | **举报与封禁** | 🔴 **上线阻塞**。线下社交产品的必答题,也是 `docs/legal` 自查清单里未打勾的一项。需要:局详情/群聊内一键举报 → 进 `reviewQueue` → 后台处理与封禁 | 1d |
| T23 | 局主工具包 | 局主可见报名者靠谱度、一键群发通知、一键改期/取消(含自动通知) | 1d |
| T21 | 运营数据看板 | 当前后台只有双指标数字,缺漏斗与趋势 | 1.5d |
| T24 | 推荐场地列表 | D09 改自由输入后降级为可选:后台维护推荐场地,发局时置顶展示以降低填写成本 | 0.5d |

> `reviewQueue` 集合已存在(内容安全命中、重复爬约会写入),T22 只需补举报入口与后台处理界面。

---

## 四、外部依赖(不依赖开发,建议立刻推进)

| # | 事项 | 阻塞什么 | 谁做 |
|---|---|---|---|
| **1** | **小程序注册 → 微信认证 → ICP 备案** | 🔴 **正式版上线**。排期上最长的杠杆 | Victor |
| **2** | **确认体验版是否也需备案** | 决定第一场活动跑体验版还是退回微信群接龙 | Victor |
| **3** | 法务审阅 `docs/legal/` | 🔴 上线。**用户协议第五条**(泰国法下的线下责任边界)与 **`gender` 必填强度**是两个重点 | 外部 |
| **4** | 品牌名 | 小程序注册与类目申报要用 | Victor |
| **5** | 曼谷场地清单 | 不阻塞(D09 已改自由输入),但推荐场地列表需要 | Victor |

---

## 五、三处待填的 TODO

| 位置 | 填什么 |
|---|---|
| `project.config.json` → `appid` | 小程序 AppID |
| `miniprogram/pages/signup-success/index.js` → `tmplIds` | 订阅消息模板 ID(成团通知) |
| `scripts/init-db.js` 输出的最后一行 | 你自己的 openid,设为 `isAdmin` + `isHost` |

`docs/legal/` 里的 `【】` 占位符(运营主体全称、联系邮箱)也需要填真实值。

---

## 六、上线前必过的自查

### 功能
- [ ] 用体验版完整跑一遍:发局 → 报名 → 成团 → 沟通 → 评价
- [ ] 造一个差 1 人的局,确认 6 小时前正确解散并收到通知
- [ ] 造一个恰好达标的局,确认成团通知发出且**不重复发**
- [ ] 确认候补递补后被递补者收到通知
- [ ] 确认归档后群聊输入框禁用、历史仍可见
- [ ] 确认注销后实测个人信息已抹除(`verifyAnonymized` 返回 clean)

### 合规
- [ ] 两地法务已审阅
- [ ] 小程序后台「用户隐私保护指引」已提交
- [ ] 报名前的同意勾选已接入,**且不可默认勾选**
- [ ] 举报入口可用(T22)
- [ ] `docs/legal/` 的占位符已填真实值

### 数据
- [ ] `init-db.js` 的集合与索引已建,**权限一律设为「仅管理端可读写」**
- [ ] `settings/global` 已初始化(`autoApprove: false`)
- [ ] 自己已设为 `isAdmin` + `isHost`

---

## 七、开发命令

```bash
npm test              # 136 项单元测试
npm run sync:config   # 部署前必须执行:同步 rules 与 common 到各云函数
node scripts/init-db.js   # 打印建库指令
```

> ⚠️ **每次部署前都要跑 `sync:config`。** 云函数是独立 node 模块,读不到目录外的文件,
> 领域逻辑靠这个脚本复制进去。忘了跑,线上跑的就是旧规则。
